# This repo has two components, each with its own env template — no values here.
#
# extension/.env.example        — the FCC extension (Go), Coston2 deployment config
# frontend/.env.local.example   — the Next.js app, incl. the F12 serverless signer key
